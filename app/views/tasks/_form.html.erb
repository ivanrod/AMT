<%= form_for [@course, @task] do |f| %>
<fieldset>
	<legend>Nueva Tarea</legend>
<%= f.label :name %>
<%= f.text_field :name %>
<%= f.label :description %>
<%= f.text_field :description %>
<%= f.label :task_type %>
<%= f.select :task_type, ["Lección","Tarea","Otros"] %>
<%= f.label :deadline %>

<%= f.text_field(:deadline, :class => "large-2 colums" ) %>

<br/>
<%= f.submit @action, :class => "button radius" %>

<% end %>

<%= link_to("Cancelar", my_courses_path, :class => "button radius alert right") %>
</fieldset>